# -*- coding: utf-8 -*-
"""棋子指南的**纯文本**——游戏里所有说明文字都在这里，别处没有第二份。

    tools/guide_text.py                     你写这里（标题 / 摘要 / 一条条的正文）
        ↓  python tools/export_guide_text.py
    neo-two-kings/data/guide_text.json      游戏读它（别手改）
        ↓  GuideText.load_text() → PieceGuide（界面代码只问 PieceGuide）
    guide.gd 排版成指南页面 / game.gd 的长按卡片

不想手写就开编辑器：`python tools/editor/editor.py` 的**「文字」页签**编辑的就是本文件，
保存时只重写下面「剧本区」那一段，文件开头的这些说明原样保留。

几条约定（写错了导出会拦下来，不会写出 JSON）：

* **一条目一行**：`points` 里每一条是一句话，界面上会自动在开头加「・」——
  所以自己**别**再写 `・`/`-`（导出会提醒你）。
* `short` 是左边栏那张速查卡上的一句话，越短越好（超过 6 个汉字导出会提醒，
  卡片只有 160 像素宽，写长了会换行、六张卡就放不下）。
* 棋子用**显示字**指认（王/弓/骑/盾/步）——和棋盘上的字、`PieceInfo.SYMBOLS` 是同一套。
* 改完**一定要跑一次导出**，否则游戏读到的还是旧的那份 JSON
  （`tests/run-tests.ps1` 第 0 步会替你查这件事）。
* 这里只管文字。**会动的棋盘**是另一条流水线（`tools/demos/*.py` → `data/guide_demos.json`），
  改它要跑 `python tools/export_guide_demos.py`。

⚠ 这里的描述必须与 `neo-two-kings/scripts/rules.gd` 保持一致：改棋规时两处一起改。
⚠ `neo-two-kings/tests/test_menu_ui.gd` 会核对：「怎么玩」第一条里写的兵力要与真实初始布局一致。
"""

from guide_text_kit import GuideText, PieceText

# --- 剧本区开始：以下由 tools/editor 改写（手改这里的格式会在下次保存时被覆盖）---
def guide_text():
    return GuideText(
        intro_title="基础玩法",
        intro_points=[
            "双方玩家各执一方棋子，目标为击杀敌方王棋",
            "双方轮流走子，默认红先绿后，每回合走一子，可向该子的周围3x3范围内任意空格移动，特殊棋子（如王、骑）有特殊走法",
            "棋子鲜有单独吃子的能力，需依靠各棋子规则排兵布阵吃子，详见下方各棋子规则",
        ],
        intro_card_glyph="棋",
        intro_card_short="规则总览",
        pieces_title="棋子细则",
        pieces=[
            PieceText(
                symbol="王",
                short="核心单位",
                tagline="王在角落的 2×2 内移动，覆盖吃子",
                points=[
                    "移动：可正常八向移动，但移动范围限制在对应出生位置的2x2区域内",
                    "攻击：无需友军协助，移动到有敌人的格子内时即对其发动攻击，王的攻击没有方向性",
                ],
            ),
            PieceText(
                symbol="弓",
                short="远程单位",
                tagline="弓需架子才可攻击，距离横竖为2斜向为1",
                points=[
                    "移动：正常八向移动",
                    "攻击：自己周围3x3范围内存在相邻友军时，向该友军方向发动攻击，攻击该方向上除友军外的格子，水平竖直方向攻击距离为2，斜向攻击距离为1",
                ],
            ),
            PieceText(
                symbol="骑",
                short="机动单位",
                tagline="骑可跳跃移动、夹击敌人",
                points=[
                    "移动1：正常八向移动",
                    "移动2：跳跃移动，自己周围3x3范围内存在单位时（友军敌军皆可），若该单位相对于本单位的后方一格为空格，则可跳跃至该空格处，一回合内可多次跳跃",
                    "攻击：自己3x3范围内存在相邻敌人时，若该敌人相对于本单位的后方一格为本单位的友军，则对该敌人发动攻击",
                ],
            ),
            PieceText(
                symbol="盾",
                short="前排单位",
                tagline="盾身后有友军时，抵挡身前攻击",
                points=[
                    "移动：正常八向移动",
                    "攻击：无主动攻击能力",
                    "抵抗：本单位即将受到攻击时，若相对于攻击来源方向的本单位的后方一格内存在友军，则抵抗本次攻击，特殊地，盾可以抵抗弓对身后队友的攻击，但无法抵抗王的攻击",
                ],
            ),
            PieceText(
                symbol="步",
                short="战斗单位",
                tagline="步的攻击为叠加攻击",
                points=[
                    "移动：正常八向移动",
                    "攻击：自己3x3范围内存在相邻敌人时，与该敌人连线，若在该直线上与本单位相连的友军比与敌人相连的敌军多，则对该相邻敌人发动攻击",
                ],
            ),
        ],
        outro_title="简要棋子规则",
        outro_points=[
            "王：在角落2x2范围内移动，通过覆盖吃子",
            "弓：需前方有友军才可攻击，水平竖直方向攻击距离为2，斜向攻击距离为1",
            "骑：当临近单位身后有空格时可跳至该空格处，同一回合内可多次连跳，夹击吃子",
            "盾：无攻击手段，当后方有友军时，抵抗前方敌军的攻击",
            "步：与敌军相连时，计算该直线上连续友军/敌军的数量，若友军数大于敌军数则发动攻击",
        ],
    )


# --- 剧本区结束 ---
