# -*- coding: utf-8 -*-
"""盾：**它不打人，它的价值是让打它的那一下作废**。五帧演完。

这是一段**纯动画**：每一帧自己写清画什么，游戏只按顺序画、不跑棋规。

摆位（0=空格）：

    0   0   步(敌) 0
    0   0   步(敌) 0
    0   0   步(敌) 0      ← 这一列三枚绿步：最下面那枚负责打，上面两枚是它的「队列」
    0   0   盾    0      ← 我的盾 (2,3)，绿步正对着它
    0   0   步    0      ← 盾正下方 (2,4) 紧挨着一枚我的步：这就是「身后」

* 第 1 帧：摆位 + 文字说清「绿步 (2,2) 朝下压着盾 (2,3)，它身后连着两枚绿步，队列 2 : 0」。
* 第 2 帧：**没有箭头**——这一发被抵抗了，文字讲「越过盾之后那一格 (2,4) 站着我自己的步」。
  棋子数不变：盾没事。
* 第 3 帧：把盾身后那枚步撤走（棋子 5 → 4），文字说局面变了。
* 第 4 帧：**一条红色箭头** (2,2)→(2,3)：同样的方向、同样的队列，只是盾身后空了。
* 第 5 帧：盾从画面上消失（棋子 4 → 3），文字点明「盾身后空着 = 白挨打」。

⚠ 「身后」是**沿着攻击方向**算的：这里敌人从上往下打，所以 (2,4) 才是盾的身后。
⚠ 五枚棋子的阵营全部写死。这张紧凑图按半场算会把敌我颠倒（好几个格子都在红方地界），
  所以每枚都写了 `red(...)` / `green(...)`——这正是当年踩过的坑。
"""

from guide_demo_kit import Demo, Frame, red, green, arrow

SYMBOL = "盾"

# --- 剧本区开始：以下由 tools/editor 改写（手改这里的格式会在下次保存时被覆盖）---
def demos():
    return [
        Demo(
            caption="盾的防御：它自己不打人，只让打它的那一下作废",
            size=7,
            frames=[
                Frame(
                    text="绿步 (2,2) 朝下压着我的盾 (2,3)：它身后连着两枚绿步，队列 2 : 0，本来打得动",
                    hold=1.0,
                    pieces={
                        (2, 0): green("步"),
                        (2, 1): green("步"),
                        (2, 2): green("步"),
                        (2, 3): red("盾"),
                        (2, 4): red("步"),
                    },
                ),
                Frame(
                    text="被抵抗：盾不吃伤害，敌人这一发白打——这就是盾的全部价值",
                    hold=1.5,
                    pieces={
                        (2, 0): green("步"),
                        (2, 1): green("步"),
                        (2, 2): green("步"),
                        (2, 3): red("盾"),
                        (2, 4): red("步"),
                    },
                ),
                Frame(
                    text="把盾身后那枚步 (2,4) 撤走：局面变了，判定条件也跟着变",
                    hold=0.714285714285714,
                    pieces={
                        (2, 0): green("步"),
                        (2, 1): green("步"),
                        (2, 2): green("步"),
                        (2, 3): red("盾"),
                    },
                ),
                Frame(
                    text="同样的方向、同样的队列，只是越过盾之后那一格空了——没人替它挡",
                    hold=1.07142857142857,
                    pieces={
                        (2, 0): green("步"),
                        (2, 1): green("步"),
                        (2, 2): green("步"),
                        (2, 3): red("盾"),
                    },
                    arrows=[
                        arrow((2, 2), (2, 3), "shot"),
                    ],
                ),
                Frame(
                    text="盾身后空着 = 白挨打：盾当场被击杀",
                    hold=0.714285714285714,
                    pieces={
                        (2, 0): green("步"),
                        (2, 1): green("步"),
                        (2, 2): green("步"),
                    },
                ),
            ],
        ),
    ]


# --- 剧本区结束 ---
