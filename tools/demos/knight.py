# -*- coding: utf-8 -*-
"""骑：两段动画，分别讲**连跳走位**与**借背后自己人击杀**。

两段都是**纯动画**：每一帧自己写清画什么，游戏只按顺序画、不跑棋规。

骑 A（连跳）摆位（0=空格）：

    0 0      0 0 0
    0 步(敌) 0 步 0
    0 0      0 0 骑

红骑 (4,2)、红步 (3,1)；绿步 (1,1)。棋盘 7×7。

* 第 1 帧：摆位。骑在 (4,2)，斜下方 (3,1) 是红步（第一块跳板），斜上方 (1,1) 是绿步（敌人）。
* 第 2 帧：**两段紫色箭头**把连跳轨迹画出来：(4,2) 越过 (3,1) 落到 (2,0)，
  再从 (2,0) 越过 (1,1) 落到 (0,2)。
* 第 3 帧：骑已经在 (0,2)。它紧邻 (1,1) 的敌人却**打不动**——因为敌人背后那一格 (2,0)
  已经空了（骑刚从那儿跳走）。要点的正是：**跳得过去 ≠ 打得到**。

骑 B（夹击）摆位（0=空格）：

    0 骑 0
    0 步(敌) 0
    0 0 步

红骑 (1,0)、红步 (2,2)；绿步 (1,1)。棋盘 7×7。

* 第 1 帧：摆位。敌人在 (1,1)，可骑在 (1,0) 还没贴上去——谁都打不到谁。
* 第 2 帧：骑**向左走一格**到 (0,0)，正好贴到敌人的斜上方。
* 第 3 帧：一条红色箭头指向 (1,1)，文字说清「敌 (1,1) 的下一个格子是 (2,2)，那里站着红步」。
* 第 4 帧：绿步从画面上消失，只剩文字点明「借力击杀」。

⚠ 与骑 A 成对看：A 讲「跳得到」，B 讲「打得动」，同样的敌人、同样的机制。
⚠ 阵营全部写死（`red("骑")` / `green("步")`）。当年这两个摆位靠 `board_pieces` 修正过：
  7×7 上 (4,2) 按半场算属绿、(1,1) 属红——照半场画就会变成「绿骑跳过两枚红步」，
  文案里的红骑与绿敌全对不上。现在阵营是显式写的，不会再有这个坑，但也**没人替你检查**了。
"""

from guide_demo_kit import Demo, Frame, red, green, arrow, rect

SYMBOL = "骑"

# --- 剧本区开始：以下由 tools/editor 改写（手改这里的格式会在下次保存时被覆盖）---
def demos():
    return [
        Demo(
            caption="骑可跳跃移动",
            size=7,
            frames=[
                Frame(
                    text="跳跃需要找架子，若架子后方为空格则可跳跃",
                    pieces={
                        (1, 1): green("步"),
                        (3, 1): red("步"),
                        (4, 2): red("骑"),
                    },
                    view=rect(0, 0, 5, 3),
                ),
                Frame(
                    text="一个回合内可多次跳跃",
                    pieces={
                        (1, 1): green("步"),
                        (3, 1): red("步"),
                        (4, 2): red("骑"),
                    },
                    highlights=[(4, 2)],
                    arrows=[
                        arrow((4, 2), (2, 0), "hop"),
                        arrow((2, 0), (0, 2), "hop"),
                    ],
                ),
                Frame(
                    text="骑跳跃两次",
                    pieces={
                        (0, 2): red("骑"),
                        (1, 1): green("步"),
                        (3, 1): red("步"),
                    },
                ),
            ],
        ),
        Demo(
            caption="骑可夹击敌人",
            size=7,
            frames=[
                Frame(
                    text="当一个敌方单位的某个方向有骑，其相反方向有骑同一阵营的单位时，会被骑夹击",
                    pieces={
                        (1, 0): red("骑"),
                        (1, 1): green("步"),
                        (2, 2): red("步"),
                    },
                    view=rect(0, 0, 3, 3),
                ),
                Frame(
                    text="骑按正常移动逻辑向左一格",
                    pieces={
                        (1, 0): red("骑"),
                        (1, 1): green("步"),
                        (2, 2): red("步"),
                    },
                    highlights=[(1, 0)],
                    arrows=[
                        arrow((1, 0), (0, 0)),
                    ],
                ),
                Frame(
                    text="骑夹击敌人",
                    pieces={
                        (0, 0): red("骑"),
                        (1, 1): green("步"),
                        (2, 2): red("步"),
                    },
                    arrows=[
                        arrow((0, 0), (1, 1), "shot"),
                        arrow((2, 2), (1, 1), "shot"),
                    ],
                ),
                Frame(
                    text="将敌人击杀",
                    pieces={
                        (0, 0): red("骑"),
                        (2, 2): red("步"),
                    },
                ),
            ],
        ),
    ]


# --- 剧本区结束 ---
