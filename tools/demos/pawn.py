# -*- coding: utf-8 -*-
"""步的教程：**走一格 → 靠队列厚度击杀**，四帧演完。

这是一段**纯动画**：每一帧自己写清画什么，游戏只按顺序画、不跑棋规。

摆位（0=空格；上面那枚步向下走一格）：

    步 0 0
    0  步 0
    0  0 步(敌)

红步(1,1)、红步(2,1)；绿步(3,3)。

* 第 1 帧：摆位。两枚红步并排在第 1 行，绿步还隔着两格——谁都打不到谁。
* 第 2 帧：一条箭头，上面那枚步向下走一格到 (2,2)。
* 第 3 帧：一条红色箭头 (2,2)→(3,3)，文字讲「比的是身后连着的同阵营棋子谁更多」。
* 第 4 帧：绿步从画面上消失，文字点明「我方 1 枚、敌方 0 枚——1 : 0，绿步被击杀」。

要点：赢的是**队列厚度**，不是「谁挨着谁」——(2,1) 那枚原封不动的红步正好排在身后，
凑出那 1 枚；把它挪走这一击就不成立了（想验证就把后面几帧里的 (2,1) 也一起删掉，
因为每帧的棋子是各写各的，不是增量）。

⚠ 棋盘 7×7 是对局尺寸；动作只占左上角 3×3 那一小块，画布也只裁到这么大。
"""

from guide_demo_kit import Demo, Frame, red, green, arrow

SYMBOL = "步"

# --- 剧本区开始：以下由 tools/editor 改写（手改这里的格式会在下次保存时被覆盖）---
def demos():
    return [
        Demo(
            caption="步的杀伤靠队列厚度：身后连得越长，越能压过对面",
            size=7,
            frames=[
                Frame(
                    text="两枚红步并排在第 1 行，绿步还隔着两格——这一步谁都打不到谁",
                    pieces={
                        (1, 1): red("步"),
                        (2, 1): red("步"),
                        (3, 3): green("步"),
                    },
                ),
                Frame(
                    text="步八方向走一格到空格：正下方那格 (2,2) 是空的",
                    pieces={
                        (1, 1): red("步"),
                        (2, 1): red("步"),
                        (3, 3): green("步"),
                    },
                    highlights=[(2, 1)],
                    arrows=[
                        arrow((2, 1), (2, 2)),
                    ],
                ),
                Frame(
                    text="步打 3×3 内每个敌人，比的是「身后连着的同阵营棋子」谁更多",
                    hold=1.2,
                    pieces={
                        (1, 1): red("步"),
                        (2, 2): red("步"),
                        (3, 3): green("步"),
                    },
                    arrows=[
                        arrow((2, 2), (3, 3), "shot"),
                    ],
                ),
                Frame(
                    text="我方身后 1 枚红步、敌方身后 0 枚——1 : 0，绿步被击杀",
                    hold=0.8,
                    pieces={
                        (1, 1): red("步"),
                        (2, 2): red("步"),
                    },
                ),
            ],
        ),
    ]


# --- 剧本区结束 ---
