# -*- coding: utf-8 -*-
"""王：**走上去直接吃子**，但活动范围被锁在自家角落的 2×2 里。

这是一段**纯动画**：`frames` 里每一帧都自己写清了「画哪几枚棋子、画哪几条箭头、
高亮哪些格子、下面写哪句话、停多久」。游戏只按顺序画，不跑棋规——你摆成什么样，
画面上就是什么样。

摆位（0=空格）：

    王 0 0
    0  王 0
    0  0 步(敌)

* 第 1 帧：王在 (0,0)，四个橙色格子画出红方的 2×2 王区，敌步正踩在对角 (1,1) 上。
* 第 2 帧：一条箭头从 (0,0) 指到 (1,1)，文字讲「王八方向走一格、走进敌人所在的格子」。
* 第 3 帧：王已经在 (1,1)、敌步退到 (2,2)：它挨在王的斜前方，却**不在王区里**，王走不过去。

要点：这三帧讲的是「王区之内能走上去吃、之外一步都去不了」。

⚠ 王区高亮是**手写**的四个格子（第 1、3 帧）。以前它来自 `Rules.king_area`，
  现在得自己填：哪天王区规则改了（比如变成 3×3），这些格子和文字都要一起改。
⚠ 敌步必须写 `green("步")`：王区整个在红方半场里（7×7 的分界是 x+y<6，王区最大和才 2），
  不写明阵营就会画成红方棋子，「吃敌人」这件事就不成立了。
"""

from guide_demo_kit import Demo, Frame, red, green, arrow

SYMBOL = "王"

# --- 剧本区开始：以下由 tools/editor 改写（手改这里的格式会在下次保存时被覆盖）---
def demos():
    return [
        Demo(
            caption="王只能在自己角落的 2×2 里挪动，靠走上去直接吃子",
            size=7,
            frames=[
                Frame(
                    text="红王在 (0,0)：橙色这一圈就是王区，红方只有左上角 2×2 这四格；敌步正踩在其中的 (1,1)",
                    pieces={
                        (0, 0): red("王"),
                        (1, 1): green("步"),
                    },
                    highlights=[(0, 0), (1, 0), (0, 1), (1, 1)],
                ),
                Frame(
                    text="王八方向走一格：(1,0)、(0,1) 是空格，而 (1,1) 上站着敌人",
                    pieces={
                        (0, 0): red("王"),
                        (1, 1): green("步"),
                    },
                    highlights=[(0, 0)],
                    arrows=[
                        arrow((0, 0), (1, 1)),
                    ],
                ),
                Frame(
                    text="棋盘拉远一格：王在 (1,1)、敌步在 (2,2)。它就在斜前方挨着，却不在王区里——王走不过去",
                    pieces={
                        (1, 1): red("王"),
                        (2, 2): green("步"),
                    },
                    highlights=[(0, 0), (1, 0), (0, 1), (1, 1)],
                ),
            ],
        ),
    ]


# --- 剧本区结束 ---
